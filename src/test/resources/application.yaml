spring:
  main:
    banner-mode: OFF
  datasource:
    url: jdbc:h2:mem:test;DB_CLOSE_ON_EXIT=FALSE;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
    hikari:
      connection-timeout: 500
server:
  servlet:
    context-path: /api